<view class="photo-container">
  <scroll-view class="movie-grid-container" scroll-y='true' bindscrolltolower="getMorePhotos">
    <block wx:for="{{photos}}" wx:key="index">
      <image src="{{item.url}}" class="photo-image" mode="widthFix"/>
    </block>
  </scroll-view>
</view>